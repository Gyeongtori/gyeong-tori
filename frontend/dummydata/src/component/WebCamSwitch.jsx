import React from 'react';

const WebcamSwitchButton = ({ switchCameraFacingMode }) => (
  <button onClick={switchCameraFacingMode}>Switch Camera Facing Mode</button>
);

export default WebcamSwitchButton;
